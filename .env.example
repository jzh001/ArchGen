# Copy to .env and fill in the keys you plan to use.
# Only keys for providers you select are read; others can stay blank.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
HUGGINGFACEHUB_API_TOKEN=
# Ollama typically runs locally and does not require a key.
